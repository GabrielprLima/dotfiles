##################################
###### Configurações gerais ######
##################################

# alias da teclas modificadora
set $mod Mod4

# Tecla para atuar com o mouse sobre janelas
floating_modifier $mod

# Habilita arrastar pela barra de título ou com a tecla modificadora
tiling_drag modifier titlebar

# foco seguir o movimento do mouse
focus_follows_mouse no



###################################
####### Espaços de trabalho #######
###################################

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10



###################################
############ Aparência ############
###################################

# Fontes das barras de título das janelas
font pango:monospace 8
#font pango:Ubuntu Bold 8
for_window [class="^.*"] border pixel 0

#for_window [class=".*"] title_format "---"

# Definições da barra de status
bar {
        status_command i3status
        position top
}

# Espaço entre as janelas e bordas da tela
gaps inner 5px

# Espaço adicional para as bordas da tela, sem afetar as bordas entre janelas
#gaps outer 5px

# Apenas a área de trabalho 3 fica sem espaçamento
#workspace 3 gaps inner 0
#workspace 3 gaps outer 0

# Ativa apenas espaços externos enquanto tiver com somente uma janela ou dividindo contêiner
smart_gaps inverse_outer

# Cores
# class                 border  backgr. text    indicator child_border
client.focused          #090909 #353535 #ffffff #808080   #353535
client.focused_inactive #333333 #070707 #ffffff #484e50   #151515
client.unfocused        #333333 #070707 #888888 #292d2e   #151515
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c

client.background       #ffffff


  
##################################
######### Teclas de ação #########
##################################

# Matar janela em foco
bindsym $mod+Shift+q kill

# Mudar foco
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternativa com as setas
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Mover janela em foco
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternativa com as setas
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Próxima janela será aberta a direita
bindsym $mod+g split h

# Próxima janela será aberta abaixo
bindsym $mod+v split v

# Janela em foco entra em modo de tela cheia
bindsym $mod+f fullscreen toggle

# Alternar layouts
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Alternar entre janela flutuante e lado a lado
bindsym $mod+Shift+space floating toggle

# Alternar foco entre janela flutuante e lado a lado
bindsym $mod+space focus mode_toggle

# Muda o foco para o contêiner pai
bindsym $mod+a focus parent

# Recarregar o arquivo de configuração
bindsym $mod+Shift+c reload
# Reiniciar a sessão preservando o ambiente
bindsym $mod+Shift+r restart
# Terminar a sessão
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# Definição do modo 'resize'
mode "resize" {
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

# Entrar no modo resize
bindsym $mod+r mode "resize"


###################################
## Teclas de início de programas ##
###################################

# Iniciar o dmenu
bindsym $mod+d exec --no-startup-id rofi -show drun

# Iniciar o terminal
bindsym $mod+Return exec i3-sensible-terminal



###################################
######## Controle de áudio ########
###################################

set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status



###################################
####### Início de programas #######
###################################

# Início de programas por lancadores .desktop (padrão XDG)
exec --no-startup-id dex --autostart --environment i3

# Início de tranca de sessão
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# Inicio do nitrogen 
exec --no-startup-id nitrogen --restore &

# Indicador de rede do Network Manager
#exec --no-startup-id nm-applet

# Início do picom
exec --no-startup-id picom --experimental-backends &
